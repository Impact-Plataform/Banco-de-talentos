const Stardex = (props)=>{
    return (
        <div className="stardex">
            {/* <Link to={`card/${props.index}`}> */}
            {props.name}
            {/* </Link> */}
        </div>
    )
}

export default Stardex