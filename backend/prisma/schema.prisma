generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id        String   @id
  name      String   @unique
  price     Float
  quantity  Int
  createdAt DateTime @default(now())

  @@map("products")
}
