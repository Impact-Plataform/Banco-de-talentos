class Validations{

          static valSymbol(symbol){
              return symbol == 'R$'
          };
      
          static valCurrency(currency){
              return currency == 'BRL'
          }
}

export default Validations;
