class Validacoes{

          static valSymbol(symbol){
              return symbol == 'R$'
          };
      
          static valCurrency(currency){
              return currency == 'BRL'
          }
}

export default Validacoes;
