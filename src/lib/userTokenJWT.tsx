/* import { useState } from "react";

interface userTokenJWTProps {
  tokenJWTRecived?: string;
  set?: boolean;
  get?: boolean;
}

const userTokenJWT = (props: userTokenJWTProps) => {
  if (props.set) {
    const [tokenJWT, setTokenJWT] = useState("");
    setTokenJWT(props.tokenJWTRecived);
    console.log(tokenJWT);
  }
  if (props.get) {
    return tokenJWT;
  } else {
    return "";
  }
};

export default userTokenJWT;
 */
